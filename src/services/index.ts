// API service functions — one file per resource.
// e.g. candidates.ts, jobs.ts, placements.ts
// Each file exports async functions typed against src/types/api.d.ts.
// Never import axios directly here — always use @/lib/api.
